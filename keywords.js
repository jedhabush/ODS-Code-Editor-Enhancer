// Simple list of SQL / ODS-related keywords to autocomplete
window.ODS_KEYWORDS = [
  "SELECT", "FROM", "WHERE", "LIMIT", "ORDER BY",
  "GROUP BY", "COUNT", "AVG", "SUM", "JOIN",
  "INNER JOIN", "LEFT JOIN", "AS", "ON", "AND", "OR"
];

// // let cmInstance = [...document.querySelectorAll('.CodeMirror')].map(el => el.CodeMirror).find(cm => cm)
